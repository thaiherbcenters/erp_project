/**
 * =============================================================================
 * EliteChequePage.jsx — หน้าหลักสำหรับบริษัท ELITE (ระบบสั่งจ่ายและพิมพ์เช็ค)
 * =============================================================================
 */

import React from 'react';
import ChequeForm from '../components/ChequeForm';

export default function EliteChequePage() {
    return <ChequeForm />;
}
