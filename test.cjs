const { sql, poolPromise } = require('./backend/config/db'); const { generateSequence } = require('./backend/utils/sequence'); async function test() { try { const pool = await poolPromise; const taskRes = await pool.request().input('TaskID', sql.VarChar, 'B20260819-WIP').query('SELECT * FROM Production_Tasks WHERE TaskID = @TaskID'); console.log(taskRes.recordset[0]); } catch(e) { console.error(e); } finally { process.exit(); } }; test();  
