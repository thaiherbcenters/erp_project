const sql = require('mssql'); require('dotenv').config(); const config = { user: process.env.DB_USER, password: process.env.DB_PASSWORD, server: process.env.DB_SERVER, database: process.env.DB_NAME, options: { encrypt: false, trustServerCertificate: true } }; async function run() { try { const pool = await sql.connect(config); await pool.request().query("INSERT INTO QuotationItem (QuotationID, ItemOrder, ItemName, Qty, Price, Amount) VALUES (55, 99, 'Test Insert', 1, 10, 10)"); const res = await pool.request().query("SELECT TOP 1 ItemName, Unit FROM QuotationItem WHERE ItemOrder = 99"); console.table(res.recordset); } catch(err) { console.error(err); } finally { sql.close(); } } run();
