require('dotenv').config({ path: 'backend/.env' }); const { poolPromise } = require('./backend/config/db'); async function test() { try { const pool = await poolPromise; const res = await pool.request().query('SELECT p.page_id, u.username FROM UserPermissions p JOIN Users u ON p.user_id = u.id WHERE u.username = ''test'''); console.log('Permissions:', res.recordset); } catch (e) { console.error(e); } process.exit(0); } test();
